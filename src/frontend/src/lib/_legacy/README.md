# Legacy — Disabled

These files are **not used** in the current application.

They handled XML ECG input parsing (sending XML files to the backend for parsing via ecg-datakit Python).
This feature was disabled for data privacy reasons: XML files may contain patient data that would transit to the server.

Kept for reference in case client-side XML parsing is implemented in the future.

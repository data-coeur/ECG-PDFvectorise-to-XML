# STEP 1: HTTP only (for certbot)
server {
    listen 80;
    server_name ecg-dev.data-coeur.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
